import React from 'react'

// hoc wrapper
import { SectionWrapper } from '@/hoc'

type Props = {}

const JiteraPage = (props: Props) => {
    return (
        <div>Jitera Page</div>
    )
}

export default SectionWrapper(JiteraPage)